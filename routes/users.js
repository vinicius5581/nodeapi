// We need to have a user an a user squema

//as a new route express has to know that we are using the express router

var router = require('express').Router();

// Encrypting our data 
//hash our password
var bcrypt = require('bcrypt.js');